@model ActiveDirectoryObjectsListModel

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <title>Все объекты домена</title>
</head>
<body>
<div>
    <h2>Объекты на домене:</h2>
    @foreach (var objectList in Model.ObjectList)
    {
        <div>
            <div>
                <span style="font-weight: bold">
                    @Html.ActionLink(objectList.DistinguishedName, "Index", "ActiveDirectory", routeValues: new {dn = objectList.DistinguishedName})
                </span>
            </div>

            <hr/>
        </div>
    }
</div>
</body>
</html>